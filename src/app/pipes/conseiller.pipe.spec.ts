import { ConseillerPipe } from './conseiller.pipe';

describe('ConseillerPipe', () => {
  it('create an instance', () => {
    const pipe = new ConseillerPipe();
    expect(pipe).toBeTruthy();
  });
});
